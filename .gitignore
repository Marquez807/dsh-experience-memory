# The DSH in-box packages are resolved by the host, not installed here. During
# development this entry is a junction into the DSH installation, so it must never
# be committed.
node_modules/

# Scratch directory used by `node tools/build.mjs --check`.
.build-check/

# Build artifacts: `lib/` IS committed (see README, "Why there is a build"), but
# the packed tarball is reproducible with `pnpm pack`.
*.tgz

# Plugin runtime data, if a test or a local run writes one here.
*.db
*.db-journal
*.db-wal

# The frozen base store a T2 sweep copies every cell from, plus its fingerprint. It is a copy
# of the live store taken at sweep start, so it is both large and machine-specific — the
# *mechanism* is committed (tools/t2-sweep.ps1), the data is not. See tools/t2-plan.md §4.17.
_frozen/

# The extracted call corpus. `tools/session-calls.py` regenerates it from session logs in
# seconds and it is tens of megabytes, so the *tool* is committed and the data is not — the
# numbers it produces are pinned in docs/DELIVERY-GAPS.md together with the command.
tools/calls.jsonl
